<template>
	<Layout>
		<Content>
			<Breadcrumb>
				<BreadcrumbItem>申请管理</BreadcrumbItem>
				<BreadcrumbItem>新增主机信息</BreadcrumbItem>
			</Breadcrumb>
			<Card>

				<Form ref="engine" :model="engineData" :rules="ruleValidate" :label-width="100">
					<Row>
						<Col span="12">
						<FormItem label="主机IP：" prop="request">
							<Input type="text"> </Input>
						</FormItem>
						</Col>
						<Col span="12">
						<FormItem label="所属集群：" prop="request">
							<Select>
								<Option value="0">全部</Option>
								<Option value="1">集群1</Option>
								<Option value="2">集群2</Option>
							</Select>
						</FormItem>
						</Col>
					</Row>
					<Row>
						<Col span="12">
						<FormItem label="型号：">
							<Input type="text"> </Input>
						</FormItem>
						</Col>
						<Col span="12">
						<FormItem label="处理器类型：">
							<Input type="text"> </Input>
						</FormItem>
						</Col>
					</Row>
					<Row>
						<Col span="12">
						<FormItem label="处理器数量：" prop="request">
							<Input type="text"> </Input>
						</FormItem>
						</Col>
						<Col span="12">
						<FormItem label="内存大小：" prop="request">
							<Input type="text"> </Input>
						</FormItem>
						</Col>
					</Row>
					<Row>
						<Col span="12">
						<FormItem label="网卡数量：" prop="request">
							<Input type="text"> </Input>
						</FormItem>
						</Col>
						<Col span="12">
						<FormItem label="磁盘大小：" prop="request">
							<Input type="text"> </Input>
						</FormItem>
						</Col>
					</Row>
				</Form>
				<div class="btngroup">
					<Button type="primary" @click="btnBack">取消</Button>
					<Button type="primary" @click="save('engine')">保存</Button>
					<Button type="primary" @click="submit('engine')">提交</Button>
				</div>
			</Card>
		</Content>
	</Layout>
</template>

<script>
	export default {
		data() {
			return {
				engineData: {},
				ruleValidate: {
					request: [{
						required: true,
						message: '该选项为必填项，请填写对应信息！',
						trigger: 'blur'
					}],
				},
			}
		},
		methods: {
			save(name) {
				this.$refs[name].validate((valid) => {
					if(valid) {
						this.$emit('examine', this.engineData);
					}
				})
			},
			submit(name) {
				this.$refs[name].validate((valid) => {
					if(valid) {
						this.$emit('examine', this.engineData);
					}
				})
			},
			btnBack() {
				this.$router.go(-1);
			},
		}
	}
</script>

<style>
.btngroup{
	text-align: center;
	margin-top: 20px;
}
</style>